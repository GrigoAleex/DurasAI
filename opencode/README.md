# OpenCode Workspace

- OpenCode-created commits in this repo must use Conventional Commits (`type(scope): subject`).
- Store AI-generated plans in `opencode/plans/`.
- Plan files in `opencode/plans/` are intentionally gitignored and must not be committed.
- Keep shareable project documentation in tracked files like `README.md` and `CONTRIBUTING.md`.
